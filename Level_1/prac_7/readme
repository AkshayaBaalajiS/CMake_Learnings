🎯 Your CMake Requirements
Parent CMake

include directories

add_subdirectory(src)

add_subdirectory(app)

src/CMakeLists.txt

Create library:

add_library(myconfig feature.cpp)


Add macro definitions:

target_compile_definitions(myconfig PRIVATE DEBUG_MODE)
target_compile_definitions(myconfig PUBLIC FEATURE_X)

app/CMakeLists.txt
add_executable(output main.cpp)
target_link_libraries(output myconfig)
